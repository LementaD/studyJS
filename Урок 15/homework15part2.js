//Додаткове завдання
// Зліпити всі завдання в один текст, таким чином, щоб спочатку відбувалося введення назви завдання, а потім за допомогою if запускався код конкретного завдання
//**************FAILTURE***********
exercise = prompt ("Введіть номер вправи")
if (exercise = 1) {// За допомогою prompt дізнайтесь число, введене користувачем. За допомогою if перевірте, чи число коректно перетворено з рядка. У разі помилки виведіть повідомлення Виведіть парне число чи ні, використовуючи if.
    let number = prompt("Введіть число:")
  if (number % 2 === 0) {
    console.log("Введене число є парним.")
  } else {
    console.log("Введене число є непарним.")
  }
}

if (exercise = 2) {// Запитайте у користувача текст і перевірте його на наявність некоректного слова або кількох некоректних слів. Використовуйте метод indexOf (або includes) рядки:
  let userInput = prompt("Введіть текст:")
  let wordsToCheck = ["матюк1", "матюк2", "матюк3"] // додайте некоректні слова, які потрібно перевірити
  if (wordsToCheck === 0) {console.log("Текст містить некоректні слова.")  
  }
  else {
  console.log("Текст не містить некоректних слів.")
  }
}

if (exercise = 3) {//   Напишіть код, який запитує ті чи інші питання з відповіддю "так"/"ні" за допомогою confirm, і зберігає відповіді в змінних.
  let answer1 = confirm("Вам подобається програмування?") // питання 1
  let answer2 = confirm("Ви впевнені у своєму виборі?") // питання 2
  console.log("Відповідь на питання 1:", answer1)
  console.log("Відповідь на питання 2:", answer2)
}

if (exercise = 4) {// Розширте попереднє завдання умовами за отриманими змінними умовами (if-else). Наприклад, якщо ви питаєте стать користувача з допомогою confirm, то за умовою зробіть alert("Ви жінка") та alert("Ви чоловік")
  let answer = confirm("Ви москаль?")
  if (answer) {alert("Мершій за кораблем")}//если "да"
  else {alert("Добридень")}//если "нет"
}

if (exercise = 5) {alert("Вправа не вирішена")
}

if (exercise = 6) {// Запитайте у користувача стать (confirm). Виведіть за допомогою alert "Ви чоловік" або "Ви жінка". Використовуйте тернарний оператор.
  let sex = confirm("Ви жінка?") ? "Ви жінка" : "Ви чоловік"
  alert(sex)
}

if (exercise = 7) {// Для завдання Number: age використовуючи АБО || вивести повідомлення про помилку (alert) якщо користувач не введе вік або натисне скасування (тобто prompt видасть порожній рядок або null, що інтерпретується як false).
  let age = prompt ("Ваш вік?")
  let year = prompt ("Який наразі рік?")
  let year_of_birth = year - age || alert("Помилка")
  alert (year_of_birth)
}

if (exercise = 8) {// За допомогою цього ж трюку (використання АБО для запуску альтернативи) зробіть примхливого робота, який у confirm запитує "шопінг?", а у разі відмови - виводити alert "ти - бяка".
  confirm("Шопінг?") || alert ("Ти - бяка")
}

if (exercise = 9) {// Зробити те саме за допомогою if.
  let answer = confirm("Шопінг?")
  if (answer) {} //если "да"
  else {alert("Ти - бяка")} //если "нет"
}

if (exercise = 10) {// Попросіть користувача ввести ПІБ в три різні змінні. Використовуючи АБО || додайте рядки за промовчанням, які будуть зберігатися у внутрішніх змінних якщо користувач ввів порожній рядок або натиснув "Скасувати". Наприклад, якщо ви на кроці введення прізвища натиснете Escape, прізвище буде "Іванов"
  answer1 = prompt ("Ваше призвище?") || "Іванов"
  answer2 = prompt ("Ваше ім'я?") || "Іван"
  answer3 = prompt ("Ви по батькові?") || "Іванович"
  console.log(answer1 || "Іванов", answer2 || "Іван", answer3 || "Іванович")
}

if (exercise = 11) {// Зробіть те саме за допомогою if та else
  answer1 = prompt ("Ваше призвище?")
  answer2 = prompt ("Ваше ім'я?")
  answer3 = prompt ("Ви по батькові?")
  if (!answer1) {answer1 = "Іванов"}
  if (!answer2) {answer2 = "Іван"}
  if (!answer3) {answer3 = "Іванович"}
  console.log(answer1, answer2, answer3)
}

if (exercise = 12) {// Напишіть код, який запитує логін, перевіряє його на вірність, якщо логін вірний, просить ввести пароль і перевіряє його. В разі розбіжності логіну чи пароля виводити alert з текстом помилки. У разі успішного логіну – alert з привітанням. Правильні логін: admin та пароль: qwerty. Використовуйте вкладені if та else.
  let login = prompt("Введіть логін:")
  if (login === "admin") {let password = prompt("Введіть пароль:")
    if (password === "qwerty") {alert("Логін та пароль вірні. Привіт!")}
    else {alert("Помилка: Невірний пароль!")}}
  else {alert("Помилка: Невірний логін!")}
}

if (exercise = 13) {// Currency exchange
  let currency = prompt("Введіть валюту (наприклад, USD, EUR):");
  let isBuying = confirm("Ви бажаєте купити? Якщо ні, то виберіть ОК для продажу, або Скасувати для виходу.");
  currency = currency.toUpperCase();
  let rate;
  if (currency === "USD") {
    rate = isBuying ? 27.5 : 27.0;
  } else if (currency === "EUR") {
    rate = isBuying ? 32.0 : 31.5;
  } else {
    alert("Введена валюта не підтримується.");
    rate = 0;
  }
  if (rate !== 0) {
    let amount = parseFloat(prompt("Введіть суму для обміну:"));
    if (isNaN(amount)) {
      alert("Введено некоректну суму.");
    } else {
      let result = isBuying ? amount * rate : amount / rate;
      alert("Результат: " + result.toFixed(2) + " " + currency);
    }
  }
}

if (exercise = 14) {// Scissors
  // Запитуємо варіант користувача
  let userChoice = prompt("Виберіть свій варіант: камінь, ножиці або папір").toLowerCase();
  // Генеруємо випадковий варіант для програми
  let computerChoice = generateComputerChoice();
  // Функція для генерації випадкового варіанту для програми
  function generateComputerChoice() {
    let randomNum = Math.random();
    if (randomNum < 0.33) {
      return "камінь";
    } else if (randomNum < 0.66) {
      return "ножиці";
    } else {
      return "папір";
    }
  }
  // Виводимо варіанти користувача і програми
  alert("Ваш вибір: " + userChoice);
  alert("Вибір програми: " + computerChoice);
  // Визначаємо переможця
  let winner;
  if (userChoice === computerChoice) {
    winner = "Нічия";
  } else if (
    (userChoice === "камінь" && computerChoice === "ножиці") ||
    (userChoice === "ножиці" && computerChoice === "папір") ||
    (userChoice === "папір" && computerChoice === "камінь")
  ) {
    winner = "Ви перемогли!";
  } else {
    winner = "Програма перемогла!";
  }
  // Виводимо ім'я переможця або "Нічия"
  alert(winner);
}